package org.mtr.neoforge;

//? if neoforge {

/*import net.neoforged.api.distmarker.Dist;
import net.neoforged.fml.ModContainer;
import net.neoforged.fml.common.Mod;
import net.neoforged.neoforge.client.gui.IConfigScreenFactory;
import org.mtr.MTR;
import org.mtr.MTRClient;
import org.mtr.screen.ConfigScreen;

@Mod(value = MTR.MOD_ID, dist = Dist.CLIENT)
public final class MTRNeoForgeClient {

	public MTRNeoForgeClient(ModContainer container) {
		MTRClient.init();
		container.registerExtensionPoint(IConfigScreenFactory.class, (IConfigScreenFactory) (modContainer, parent) -> new ConfigScreen(parent));
	}
}

*///? }
